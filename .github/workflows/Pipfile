[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[requires]
python_version = "3.8"  # Adjust to your desired Python version

[packages]
pytest = "*"  # For testing
build = "*"   # For packaging
twine = "*"   # For uploading to PyPI
click = "*"   # If using Click for CLI

[dev-packages]
pytest-cov = "*"  # Code coverage for testing

[scripts]
test = "pytest tests/"